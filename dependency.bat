@echo off
echo Installing dependencies...

rem Install Pillow (for image processing)
pip install pillow

rem Install OpenCV (for image manipulation)
pip install opencv-python

echo All dependencies installed. You can now run the script.
pause
